# Install Script Multi-Instance Support

- [x] Review the current single-instance assumptions in `install.sh`.
- [x] Refactor `install.sh` to derive install paths and systemd service names from an instance identifier while preserving default-instance compatibility.
- [x] Validate the updated script syntax and spot-check the multi-instance flow.
